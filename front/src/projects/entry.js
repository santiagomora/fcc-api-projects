
import headerProject from './header';

import shortenerProject from './shortener';

import timestampProject from './timestamp';

const projects = [
    timestampProject,
    headerProject,
    shortenerProject
];

export default projects;
